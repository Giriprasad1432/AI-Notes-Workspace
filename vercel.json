{
  "version": 2,
  "builds": [
    {
      "src": "frontend/ai note saver/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/frontend/ai note saver/dist/$1" }
  ]
}
