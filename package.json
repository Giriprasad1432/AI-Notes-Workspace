{
  "name": "ai-notes-workspace",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend/ai-note-saver && npm ci && npm run build",
    "build:backend": "cd backend && npm ci && npm run build || true",
    "build": "npm run build:frontend"
  }
}
